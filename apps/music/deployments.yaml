apiVersion: apps/v1
kind: Deployment
metadata:
  name: qbittorrent
  namespace: music
  labels:
    app.kubernetes.io/name: qbittorrent
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: qbittorrent
  template:
    metadata:
      labels:
        app.kubernetes.io/name: qbittorrent
      annotations:
        homelab.oisd.io/root-container-justification: LinuxServer s6 init requires root; PUID/PGID drops the service to UID/GID 1000.
    spec:
      serviceAccountName: qbittorrent
      automountServiceAccountToken: false
      securityContext:
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: qbittorrent
          image: lscr.io/linuxserver/qbittorrent:5.2.3_v2.0.14-ls472@sha256:212b86dff59e3962b4082b5ef20a577e76c8f8527d2ab505cfa887b4bcecb0b0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: TZ
              value: Europe/Paris
            - name: WEBUI_PORT
              value: "8080"
          startupProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: config
              mountPath: /config
            - name: nas
              mountPath: /nas
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: qbittorrent-config
        - name: nas
          nfs:
            server: 192.168.2.1
            path: /srv/nfs/raid
            readOnly: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prowlarr
  namespace: music
  labels:
    app.kubernetes.io/name: prowlarr
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: prowlarr
  template:
    metadata:
      labels:
        app.kubernetes.io/name: prowlarr
      annotations:
        homelab.oisd.io/root-container-justification: LinuxServer s6 init requires root; PUID/PGID drops the service to UID/GID 1000.
    spec:
      serviceAccountName: prowlarr
      automountServiceAccountToken: false
      securityContext:
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: prowlarr
          image: lscr.io/linuxserver/prowlarr:2.5.2.5491-ls156@sha256:1295cff29d10b486c0d8324d1559a552140a5932bf8b3d87e398654414f63f92
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 9696
              protocol: TCP
          env:
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: TZ
              value: Europe/Paris
          startupProbe:
            httpGet:
              path: /ping
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /ping
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /ping
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: config
              mountPath: /config
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: prowlarr-config
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flaresolverr
  namespace: music
  labels:
    app.kubernetes.io/name: flaresolverr
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: flaresolverr
  template:
    metadata:
      labels:
        app.kubernetes.io/name: flaresolverr
      annotations:
        homelab.oisd.io/request-basis: Music pods measured at 1-13m CPU and 13-154Mi memory; 100m and 256Mi cover current steady use.
        homelab.oisd.io/limit-basis: One CPU and 1Gi memory are initial Chromium challenge tripwires; remeasure after the first successful solve.
    spec:
      serviceAccountName: flaresolverr
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: flaresolverr
          image: ghcr.io/flaresolverr/flaresolverr:v3.5.0@sha256:139dfee1c6f89249c8d665d1333a42e8ec74ec0a86bc6bb1c8461e10d3a66a47
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8191
              protocol: TCP
          env:
            - name: LOG_LEVEL
              value: info
            - name: TZ
              value: Europe/Paris
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lidarr
  namespace: music
  labels:
    app.kubernetes.io/name: lidarr
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: lidarr
  template:
    metadata:
      labels:
        app.kubernetes.io/name: lidarr
      annotations:
        homelab.oisd.io/root-container-justification: LinuxServer s6 init requires root; PUID/PGID drops the service to UID/GID 1000.
    spec:
      serviceAccountName: lidarr
      automountServiceAccountToken: false
      securityContext:
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: lidarr
          image: lscr.io/linuxserver/lidarr:3.1.0.4875-ls39@sha256:2e4cdc7c8d5fa36915f446a29976ee01d3f9cc9722b8530a09121c76fbabef55
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8686
              protocol: TCP
          env:
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: TZ
              value: Europe/Paris
          startupProbe:
            httpGet:
              path: /ping
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /ping
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /ping
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: config
              mountPath: /config
            - name: nas
              mountPath: /nas
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: lidarr-config
        - name: nas
          nfs:
            server: 192.168.2.1
            path: /srv/nfs/raid
            readOnly: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: navidrome
  namespace: music
  labels:
    app.kubernetes.io/name: navidrome
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: navidrome
  template:
    metadata:
      labels:
        app.kubernetes.io/name: navidrome
    spec:
      serviceAccountName: navidrome
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: navidrome
          image: deluan/navidrome:0.63.2@sha256:9012939114fbb1bb641b81cf96dec5ded15f0aafefe8d47a511d7cb919658e40
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 4533
              protocol: TCP
          env:
            - name: ND_PORT
              value: "4533"
            - name: ND_SCANINTERVAL
              value: 1h
            - name: ND_MUSICFOLDER
              value: /nas/multimedia/Music
            - name: ND_DATAFOLDER
              value: /data
            - name: ND_ADDRESS
              value: 0.0.0.0
            - name: ND_LOGLEVEL
              value: info
            - name: TZ
              value: Europe/Paris
          startupProbe:
            httpGet:
              path: /ping
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /ping
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /ping
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          volumeMounts:
            - name: data
              mountPath: /data
            - name: music
              mountPath: /nas
              readOnly: true
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: navidrome-data
        - name: music
          nfs:
            server: 192.168.2.1
            path: /srv/nfs/raid
            readOnly: true
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: slskd
  namespace: music
  labels:
    app.kubernetes.io/name: slskd
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: slskd
  template:
    metadata:
      labels:
        app.kubernetes.io/name: slskd
      annotations:
        homelab.oisd.io/request-basis: Music pods measured at 1-3m CPU and 28-317Mi memory; 100m and 256Mi are initial scheduling requests for the first transfer test.
        homelab.oisd.io/limit-basis: One CPU and 1Gi memory are tripwires above the measured music workload; remeasure slskd after a completed transfer.
    spec:
      serviceAccountName: slskd
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: slskd
          image: slskd/slskd:0.25.1
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 5030
              protocol: TCP
            - name: soulseek
              containerPort: 50300
              protocol: TCP
          env:
            - name: SLSKD_USERNAME
              valueFrom:
                secretKeyRef:
                  name: slskd-credentials
                  key: SLSKD_USERNAME
            - name: SLSKD_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: slskd-credentials
                  key: SLSKD_PASSWORD
            - name: SLSKD_API_KEY
              valueFrom:
                secretKeyRef:
                  name: slskd-credentials
                  key: SLSKD_API_KEY
            - name: SLSKD_JWT_KEY
              valueFrom:
                secretKeyRef:
                  name: slskd-credentials
                  key: SLSKD_JWT_KEY
            - name: SLSKD_REMOTE_CONFIGURATION
              value: "true"
            - name: SLSKD_REMOTE_FILE_MANAGEMENT
              value: "false"
            - name: SLSKD_DOWNLOADS_DIR
              value: /nas/multimedia/Downloads/slskd/complete
            - name: SLSKD_INCOMPLETE_DIR
              value: /nas/multimedia/Downloads/slskd/incomplete
          startupProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
          readinessProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          volumeMounts:
            - name: config
              mountPath: /app
            - name: downloads
              mountPath: /nas/multimedia/Downloads/slskd
              subPath: multimedia/Downloads/slskd
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: slskd-config
        - name: downloads
          nfs:
            server: 192.168.2.1
            path: /srv/nfs/raid
            readOnly: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: soularr
  namespace: music
  labels:
    app.kubernetes.io/name: soularr
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: soularr
  template:
    metadata:
      labels:
        app.kubernetes.io/name: soularr
      annotations:
        homelab.oisd.io/request-basis: The initial workload is one missing album per scan; 50m CPU and 128Mi memory are starting requests to remeasure after the first import.
        homelab.oisd.io/limit-basis: 500m CPU and 512Mi memory are tripwires above the measured music automation workload.
    spec:
      serviceAccountName: soularr
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: soularr
          image: mrusse/soularr:1.2.2
          imagePullPolicy: IfNotPresent
          env:
            - name: TZ
              value: Europe/Paris
            - name: SCRIPT_INTERVAL
              value: "300"
            - name: WEBUI_ENABLED
              value: "false"
          startupProbe:
            exec:
              command: [/bin/sh, -c, "ps aux | grep -q '[r]un.sh'"]
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
          readinessProbe:
            exec:
              command: [/bin/sh, -c, "ps aux | grep -q '[r]un.sh'"]
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            exec:
              command: [/bin/sh, -c, "ps aux | grep -q '[r]un.sh'"]
            periodSeconds: 60
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          volumeMounts:
            - name: data
              mountPath: /data
            - name: config
              mountPath: /data/config.ini
              subPath: config.ini
              readOnly: true
            - name: downloads
              mountPath: /nas/multimedia/Downloads/slskd/complete
              subPath: multimedia/Downloads/slskd/complete
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: soularr-data
        - name: config
          secret:
            secretName: soularr-config
        - name: downloads
          nfs:
            server: 192.168.2.1
            path: /srv/nfs/raid
            readOnly: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: musicgrabber
  namespace: music
  labels:
    app.kubernetes.io/name: musicgrabber
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: musicgrabber
  template:
    metadata:
      labels:
        app.kubernetes.io/name: musicgrabber
      annotations:
        homelab.oisd.io/request-basis: Idle v4.0.2 measured at 2.2m CPU and 146Mi memory; 25m and 256Mi cover the measured idle workload.
        homelab.oisd.io/limit-basis: Upstream requires 2Gi shared memory for Spotify playlists over 100 tracks; 3Gi covers that plus the measured 146Mi process and 854Mi headroom. Browser-import CPU has not been measured, so it has no speculative limit.
        homelab.oisd.io/search-concurrency-basis: Five is MusicGrabber's enforced maximum and reduces provider admission blocking for the measured 1,599-track import.
        homelab.oisd.io/root-container-justification: The entrypoint needs root to create UID/GID 1000, chown persistent data, and then use gosu to start MusicGrabber as UID/GID 1000.
    spec:
      serviceAccountName: musicgrabber
      automountServiceAccountToken: false
      securityContext:
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: musicgrabber
          image: g33kphr33k/musicgrabber:4.0.2@sha256:9e280b70d6a0b324dbf4cfe59e482b8f0693bdf7171f55b551c78d02c8f9cb53
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: TZ
              value: Europe/Paris
            - name: MUSIC_DIR
              value: /music
            - name: DB_PATH
              value: /data/music_grabber.db
            - name: DEFAULT_CONVERT_AUDIO
              value: "false"
            - name: ENABLE_MUSICBRAINZ
              value: "true"
            - name: SEARCH_CONCURRENCY
              value: "5"
            - name: SOURCE_SOULSEEK_ENABLED
              value: "true"
            - name: SLSKD_URL
              value: http://slskd.music.svc.cluster.local:5030
            - name: SLSKD_USER
              valueFrom:
                secretKeyRef:
                  name: slskd-credentials
                  key: SLSKD_USERNAME
            - name: SLSKD_PASS
              valueFrom:
                secretKeyRef:
                  name: slskd-credentials
                  key: SLSKD_PASSWORD
            - name: SLSKD_DOWNLOADS_PATH
              value: /downloads
          startupProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 24
          readinessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 25m
              memory: 256Mi
            limits:
              memory: 3Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
              add: [CHOWN, SETGID, SETUID]
          volumeMounts:
            - name: data
              mountPath: /data
            - name: nas
              mountPath: /music
              subPath: multimedia/Music
            - name: nas
              mountPath: /downloads
              subPath: multimedia/Downloads/slskd/complete
              readOnly: true
            - name: shared-memory
              mountPath: /dev/shm
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: musicgrabber-data
        - name: nas
          nfs:
            server: 192.168.2.1
            path: /srv/nfs/raid
            readOnly: false
        - name: shared-memory
          emptyDir:
            medium: Memory
            sizeLimit: 2Gi
